{
  "name": "interface-mode",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:pages": "tsc -b && vite build --mode pages",
    "build:lib": "tsc -b && vite build --mode lib",
    "deploy:blog": "node scripts/deploy-blog.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  },
  "dependencies": {
    "marked": "^18.0.5"
  }
}
